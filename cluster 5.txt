Microsoft Windows [Version 10.0.26100.6584]
(c) Microsoft Corporation. All rights reserved.

C:\Windows\System32>spark-shell
25/10/15 13:13:56 WARN Shell: Did not find winutils.exe: java.io.FileNotFoundException: Hadoop bin directory does not exist: C:\hadoopsetup\hadoop-3.2.4\bin -see https://wiki.apache.org/hadoop/WindowsProblems
Setting default log level to "WARN".
To adjust logging level use sc.setLogLevel(newLevel). For SparkR, use setLogLevel(newLevel).
Spark context Web UI available at http://luckysteven:4040
Spark context available as 'sc' (master = local[*], app id = local-1760514243858).
Spark session available as 'spark'.
Welcome to
      ____              __
     / __/__  ___ _____/ /__
    _\ \/ _ \/ _ `/ __/  '_/
   /___/ .__/\_,_/_/ /_/\_\   version 3.5.6
      /_/

Using Scala version 2.12.18 (Java HotSpot(TM) 64-Bit Server VM, Java 1.8.0_441)
Type in expressions to have them evaluated.
Type :help for more information.

scala> import org.apache.spark._
import org.apache.spark._

scala> import org.apache.spark.rdd.RDD
import org.apache.spark.rdd.RDD

scala>

scala> import org.apache.spark.graphx._
import org.apache.spark.graphx._

scala> :paste
// Entering paste mode (ctrl-D to finish)

val vertices = Array((1L, ("A")), (2L, ("B")),(3L,("C")))

val vRDD= sc.parallelize(vertices)

 vRDD.take(1)
val vertices = Array((1L, ("A")), (2L, ("B")),(3L,("C")))

val vRDD= sc.parallelize(vertices)

 vRDD.take(1)


// Exiting paste mode, now interpreting.

<pastie>:35: error: vertices is already defined as value vertices
val vertices = Array((1L, ("A")), (2L, ("B")),(3L,("C")))
    ^
<pastie>:37: error: vRDD is already defined as value vRDD
val vRDD= sc.parallelize(vertices)
    ^

scala> :paste
// Entering paste mode (ctrl-D to finish)

val vertices = Array((1L, ("A")), (2L, ("B")),(3L,("C")))

val vRDD= sc.parallelize(vertices)

 vRDD.take(1)

// Exiting paste mode, now interpreting.

vertices: Array[(Long, String)] = Array((1,A), (2,B), (3,C))
vRDD: org.apache.spark.rdd.RDD[(Long, String)] = ParallelCollectionRDD[0] at parallelize at <pastie>:32
res1: Array[(Long, String)] = Array((1,A))

scala> :paste
// Entering paste mode (ctrl-D to finish)

 val edges = Array(Edge(1L,2L,1800),Edge(2L,3L,800),Edge(3L,1L,1400))

 val eRDD= sc.parallelize(edges)

eRDD.take(2)


// Exiting paste mode, now interpreting.

edges: Array[org.apache.spark.graphx.Edge[Int]] = Array(Edge(1,2,1800), Edge(2,3,800), Edge(3,1,1400))
eRDD: org.apache.spark.rdd.RDD[org.apache.spark.graphx.Edge[Int]] = ParallelCollectionRDD[1] at parallelize at <pastie>:32
res2: Array[org.apache.spark.graphx.Edge[Int]] = Array(Edge(1,2,1800), Edge(2,3,800))

scala> val nowhere = "nowhere"
nowhere: String = nowhere

scala>

scala>  val graph = Graph(vRDD,eRDD,nowhere)
graph: org.apache.spark.graphx.Graph[String,Int] = org.apache.spark.graphx.impl.GraphImpl@2afdfcc3

scala>

scala>  graph.vertices.collect.foreach(println)
(1,A)
(2,B)
(3,C)

scala>

scala>  graph.edges.collect.foreach(println)
Edge(1,2,1800)
Edge(2,3,800)
Edge(3,1,1400)

scala>

scala>  val numairports = graph.numVertices
numairports: Long = 3

scala>

scala> val numroutes = graph.numEdges
numroutes: Long = 3

scala>

scala>  graph.edges.filter { case Edge(src, dst, prop) => prop > 1000}.collect.foreach(println)
Edge(1,2,1800)
Edge(3,1,1400)

scala>

scala>  graph.triplets.take(3).foreach(println)
((1,A),(2,B),1800)
((2,B),(3,C),800)
((3,C),(1,A),1400)

scala>

scala>  val f = graph.inDegrees
f: org.apache.spark.graphx.VertexRDD[Int] = VertexRDDImpl[26] at RDD at VertexRDD.scala:57

scala>

scala>  f.collect()
res7: Array[(org.apache.spark.graphx.VertexId, Int)] = Array((1,1), (2,1), (3,1))

scala>

scala> val totalDegrees = graph.degrees
totalDegrees: org.apache.spark.graphx.VertexRDD[Int] = VertexRDDImpl[30] at RDD at VertexRDD.scala:57

scala>